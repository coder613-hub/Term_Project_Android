<?xml version="1.0" encoding="utf-8"?>
<resources>
        <color name="background_default_color">@color/secondaryDarkColor</color>

        <!-- For colored text; default is black/dark-gray (Day) or white/light-gray (Night) -->
        <color name="primary_text_colored_color">@color/primaryLightColor</color>
        <color name="secondary_text_colored_color">@color/primaryColor</color>

        <!-- Matching colored background -->
        <color name="background_colored_color">@color/primaryDarkColor</color>

</resources>